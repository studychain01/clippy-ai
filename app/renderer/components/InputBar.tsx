/**
 * INPUT BAR (controlled form)
 * - Text input + Send button
 * - Calls onSubmit when user presses Enter or clicks Send
 * - Can disable while loading
 *
 * Props:
 *   onSubmit: (text: string) => void
 *   loading?: boolean
 */
