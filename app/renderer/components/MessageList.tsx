/**
 * MESSAGE LIST
 * - Renders messages in order (user vs assistant styling).
 * - Auto-scroll to bottom (later via useRef/useEffect).
 *
 * Props:
 *   messages: Array<{ id, role, content, ts }>
 */
